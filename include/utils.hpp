#ifndef UTILS_HPP
#define UTILS_HPP

enum Difficulty {
	LOWER = -1,
	NORMAL = 0,
	HIGHER = 1
};

void print_screen_seperator();

void clr_scr();

void set_terminal_size();

void continue_promt();

#endif //UTILS_HPP
